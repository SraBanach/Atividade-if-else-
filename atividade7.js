console.log ("Escreva um programa que leia dois números e uma operação")
console.log ("(adição, subtração, multiplicação, divisão) e realize a operação entre os dois números.)")

num1 = 2
num2 = 2
operacao = "x"


if (operacao == "+"){
        console.log (" A soma entre os dois numeros é", (num1 + num2) )
    }
    else if (operacao == "-"){
        console.log (" A subtração entre os dois numeros é", (num1 - num2) )
    }
    else if (operacao == "x"){
        console.log (" A multiplicação entre os dois numeros é", (num1 * num2) )
    }
    else if (operacao == "/"){
        console.log (" A divisão entre os dois numeros é", (num1 / num2) )
    }
