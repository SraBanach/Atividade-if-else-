console.log("Escreva um programa que verifique se uma letra fornecida pelo usuário é uma vogal ou uma consoante.")
// colocar tolowercase() para pegar a letra tanto maiuscula quanto minuscula

letra = "A"

if (letra.toLowerCase() == "a" | "e" | "i" | "o" | "u") {
    console.log ("É uma vogal")
}
else {
    console.log ("É uma consoante ")
}
